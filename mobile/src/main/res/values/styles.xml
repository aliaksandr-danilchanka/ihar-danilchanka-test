<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/global_color_primary</item>
        <item name="colorPrimaryDark">@color/global_color_primary_dark</item>
        <item name="colorAccent">@color/global_color_primary</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:textColor">@color/global_text_primary</item>
    </style>

    <style name="PopUpTheme" parent="Theme.AppCompat">
        <item name="android:textColor">@color/global_text_primary</item>
        <item name="android:itemBackground">@drawable/btn_white</item>
    </style>
</resources>
